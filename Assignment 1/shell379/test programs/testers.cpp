#include <iostream>
#include <iostream>
#include <unistd.h>
#include <sys/wait.h>
#include <stdio.h>
#include <map>
#include <time.h>
#include <vector>
using namespace std;

int main(int argc, char argv[])
{
    // for (int i = 0; i < argc; i++)
    // {
    //     cout << "Argument " << i << " = " << argv[i] << endl;
    // }
    // string userInput;
    // getline(cin, userInput);
    // cout << "CIN: " << userInput << endl;
    // sleep(2);
    int i;
    float p;

    vector<int> a;
    vector<float> b;
    while (p == p)
    {
        (p++) + 1.144564542315345;
        (i++) + 1;
        a.push_back(i);
        b.push_back(p);

        if (i > 99999999)
            break;
    }
    cout << "DONE" << endl;
    return 12345;
}